package com.feelcondorinc.integraservicios.entities.models;

/**
 * La enumeración RolUsuario representa los roles que pueden tener los usuarios en el sistema.
 */
public enum RolUsuario {
    /**
     * Indica que el usuario tiene el rol de administrador.
     */
    ADMIN,

    /**
     * Indica que el usuario tiene el rol de empleado.
     */
    EMPLEADO,

    /**
     * Indica que el usuario tiene el rol de afiliado.
     */
    AFILIADO
}
